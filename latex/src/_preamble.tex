\documentclass[12pt, a4paper,titlepage]{book}
\setlength{\headheight}{15pt}

\usepackage[a4paper,left=2.5cm,right=2.5cm,top=2.5cm,bottom=2.5cm]{geometry}
\usepackage[english]{babel}
\usepackage[utf8]{inputenc}
\usepackage[debugshow]{tabularx}
\usepackage{hyperref}
\hypersetup{
    hidelinks = true,
    linktoc = all, 
}

\usepackage{naive-ebnf}
\usepackage{
	wrapfig,
	graphicx,
	titlesec,
	fancyhdr,
	pgfplots,
	tikz,
	amsmath,
	amsthm,
	subcaption,
        mathtools,
	multirow,
	listings,
	fancybox,
	xcolor,
	environ,
	amssymb,
	minted,
	forest,
	bm, 
	xifthen,
	xparse
}

\usepackage[capitalize]{cleveref}

% -- Font settings
\usepackage[math-style=ISO]{unicode-math}
\setmainfont{EB Garamond}%You should have installed the font
\setmathfont{Garamond-Math.otf}[StylisticSet={8,9}]
\setmathfont{Garamond-Math.otf}[range={scr,bfscr}]
% -- Tikz settings
\usetikzlibrary{
  matrix,
  chains,
  positioning,
  decorations.pathreplacing,
  arrows,
  angles,
  quotes,
  shapes.geometric,
  plotmarks,
  calc
}
\pgfplotsset{compat=newest}
\usepgfplotslibrary{fillbetween, colormaps}

% -- something wrong with accents kerning...
\let\orghat\hat
\def\hat#1{\orghat{\kern0pt #1}}

% -- Theorems, Lemmas, Corollaries, Observations, Definitions
\newtheorem{thm}{Theorem}[section]
\newtheorem{lem}{Lemma}[section]
\newtheorem{cor}{Corollary}[section]
\newtheorem{obs}{Observation}[section]
\theoremstyle{definition}
\newtheorem{defn}{Definition}[section]

% Chapter Titling: Chapter [0-9] LEFT, Chapter Title RIGHT
\newcommand*{\justifyheading}{\raggedleft}
\titleformat{\chapter}[display]
{\normalfont\large}{\MakeUppercase\chaptertitlename \ \ \thechapter}
{20pt}{\Huge\bfseries\justifyheading}

% Set space between item separators in enumerate/itemize envs.
\setlength\itemsep{0pt}

\setcounter{tocdepth}{4}
\setcounter{secnumdepth}{4}

\begin{document}
